[project]
name = "seed-dev"
version = "0.1.0"
description = "Seeding support for the supabase dev environment"
requires-python = ">=3.11"

[tool.pixi.project]
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]
